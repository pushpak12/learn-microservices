spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/pushpak12/microservice-configuration
          clone-on-start: true

server:
  port: 8085
